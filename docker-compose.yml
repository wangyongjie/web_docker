version: "3"
services:
  nginx:
    image: nginx
    build: ./nginx
    container_name: nginx_website
    volumes:
      - /deploy:/deploy
      - /deploy/docker-compose/nginx/conf.d:/etc/nginx/conf.d
    restart: always
